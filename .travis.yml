sudo: required
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libusb-1.0-0-dev qtbase5-dev qtbase5-dev-tools qtscript5-dev libqt5svg5-dev qttools5-dev libpoppler-qt5-dev

language: cpp

# Compiler selection
compiler:
  - gcc

install: true

# Build steps
script:
  - make gui-tools
