#!/bin/bash

args=""
for arg in "$@"
do
	if [[ $arg == *" "* ]]
	then
		args+=" \"$arg\""
	else
		args+=" $arg"
	fi
done

DIR=$(dirname "$0")
CMD="php \"$DIR/../scripts/gpblock.php\" -a "
CMD+=$args
eval $CMD
